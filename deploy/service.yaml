apiVersion: v1
kind: Service
metadata:
  name: deepinsight-server
  namespace: rag-etl
  labels:
    app: deepinsight-server
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: deepinsight-server
