# Secret Template — apply manually, NEVER commit real values
apiVersion: v1
kind: Secret
metadata:
  name: deepinsight-secrets
  namespace: rag-etl
type: Opaque
stringData:
  llm-api-key: "<YOUR_LLM_API_KEY>"
  llm-base-url: "<YOUR_LLM_BASE_URL>"
